<h1>Physician#new</h1>
<%= physician_path(model: @physician, local: true, class: "ui form") do |f| %>
  <div class="field"> 
  <%= f.label :name %>
  <%= f.text_field :name, required: true, placeholder: "Name" %>
  </div>
  <%= f.submit nil, class: "ui green button inverted" %>
  <% end %>
